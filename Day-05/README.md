The folder contains 2 jupyter files and one dataset. One jupyter file shows the classification of ANN on MNIST which is a popular dataset containing hand drawn numbers.
The other jupyter file shows Masters admission prediction using ANN and it uses an external dataset which is also attached.
